namespace Assets.Scripts.Towers
{
    public class GokuSSJ3 : Tower
    {
        // Start is called before the first frame update
        void Start()
        {
        }

        // Update is called once per frame
        //void Update()
        //{

        //}
    }
}